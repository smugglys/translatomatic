comment parsing currently not working with:
- plist files (parser doesn't return comments)
- yaml files (parser doesn't return comments)
- subtitles (parser doesn't include enough information to determine if strings are comments or subtitle text)
